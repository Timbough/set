{selector
 	{type "cannon_shell"}
	{group
		{sort {class tank}}
		{sort {class armoredcar}}
		{sort {class car}}
		{sort {class airborne}}
		{sort {weapon "fg"}}
		{sort {weapon "sr"}}
		{sort {weapon "wp"}}
		{sort {weapon}}
		{sort distance}
	}

	{group
		{sort {attacking}}
		{sort distance}
	}
}

