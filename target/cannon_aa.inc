{selector
 	{type "cannon_aa"}
	{group
		{sort {class attacker}}
		{sort {class fighter}}
		{sort {class transport}}	
		{sort {class tank}}
		{sort {class armoredcar}}
		{sort {class car}}
		{sort {weapon "zenite"}}
		{sort {weapon "fg"}}
		{sort {weapon "ap"}}
		{sort {weapon}}
		{sort distance}
	}

	{group
		{sort {attacking}}
		{sort distance}
	}
}

