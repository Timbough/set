{selector
 	{type "tank_bullet_aa"}

	{group
		{sort {class attacker}}
		{sort {class fighter}}
		{sort {class human} {weapon}}
		{sort {class human}}
		{sort {class transport}}
		{sort distance}
	}
}
