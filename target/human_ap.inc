{selector
	{unit "human" "ap"}

	{group
		;{ignore {class airborne}}
		{sort {class tank}}
		{sort {class armoredcar}}
		{sort {class car}}
		{sort {class airborne}}
		{sort {attacking}}
		{sort distance}
    }
}