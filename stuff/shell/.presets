(include "\set\explosives.accuracy")

(define "_tr"
	{trace
		{view "/texture/fx/trace"
			{Light 8}
			{ScaleY 0.35}
		}
		{length (* %0 12)}
	}
)
(define "_tr2"
	{trace
		{view "/texture/fx/trace_apcr"
			{Light 8}
			{ScaleY 0.35}
		}
		{length (* %0 12)}
	}
)